package install

import "github.com/DryBearr/dryserver/internal/runner"

// Aliases so installer code reads naturally.
type (
	Cmd        = runner.Cmd
	Runner     = runner.Runner
	ExecRunner = runner.ExecRunner
)
